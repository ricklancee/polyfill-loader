{
  "name": "polyfill-loader",
  "version": "0.1.0",
  "description": "A simple browser script to test and load polyfills asynchronously",
  "scripts": {
    "build": "./node_modules/.bin/uglifyjs polyfill.js --comments --mangle --output polyfill.min.js"
  },
  "author": "Rick Lancee",
  "license": "MIT",
  "devDependencies": {
    "uglify-js": "^2.7.4"
  }
}
